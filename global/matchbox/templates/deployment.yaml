kind: Deployment
apiVersion: extensions/v1beta1

metadata:
  name: matchbox-deployment

spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: matchbox

    spec:
      volumes:
      {{- range $region,$region-content := .Values.regions }}
        - name: certs-{{ $region }}
          secret:
            secretName: certs-{{ $region }}
        - name: config-{{ $region }}
          configMap:
            name: config-{{ $region }}
      {{- end }}
      containers:
        - name: matchbox
          image: hub.global.cloud.sap/monsoon/matchbox:v0.6.1
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            timeoutSeconds: 5
          volumeMounts:
          {{- range $region,$region-content := .Values.regions }}
            - name: certs-{{ $region }}
              mountPath: /var/lib/matchbox/assets/kubernetes/ssl/{{ $region }}
            - name: config-{{ $region }}
              mountPath: /var/lib/matchbox/groups
          {{- end }}
