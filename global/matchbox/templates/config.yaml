apiVersion: v1
kind: ConfigMap
metadata:
  name: config-{{ .Release.Name }}
  labels:
    app: matchbox

data:
{{- range $key,$value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
