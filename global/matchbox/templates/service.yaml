apiVersion: v1
kind: Service
metadata:
  name: matchbox
  labels:
    app: matchbox

spec:
  selector:
    app: matchbox
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
