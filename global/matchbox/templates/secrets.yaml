{{- range $i,$s := .Values.regions }}
apiVersion: v1
kind: Secret
metadata:
  name: certs-{{ $i }}
  labels:
    app: matchbox
type: Opaque
data:
  {{- range $t := $s.certs }}
    {{- range $u,$v := $t }}
  - {{ $u }}: {{ $v | b64enc | quote }}
    {{- end }}
  {{- end }}
---
{{- end }}
