apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-snmp

data:
  prometheus.yaml: |
    global:
      scrape_timeout: 55s

    scrape_configs:
    - job_name: snmp-baremetal
      file_sd_configs:
        - files:
          - /etc/snmp_exporter/targets.json
