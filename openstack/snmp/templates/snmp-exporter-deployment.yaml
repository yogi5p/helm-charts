apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ template "snmp-exporter.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}

spec:
  revisionHistoryLimit: 5
  replicas: {{ .Values.snmp_exporter.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ template "snmp-exporter.name" . }}
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: {{ .Values.snmp_exporter.service.port | quote }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.snmp_exporter.image.repository }}:{{ .Values.snmp_exporter.image.tag }}"
          imagePullPolicy: {{ .Values.snmp_exporter.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.snmp_exporter.service.port }}
          args:
            - --config.file=/etc/snmp_exporter/snmp.yml
          volumeMounts:
            - name: config
              mountPath: /etc/snmp_exporter
      volumes:
        - name: config
          configMap:
            name: {{ .Chart.Name }}
